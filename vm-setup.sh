
#run this script to set up environmental variables. We'll also put code in here to fix issues with the virtual machine

#turn off lock screen
gsettings set org.gnome.desktop.lockdown disable-lock-screen 'true'

#add an alias
alias l='ls -ltrh'
